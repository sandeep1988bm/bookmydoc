apiVersion: app/v1
kind: Deployment
metadata:
  name: angular-deployment
spec:
  selector:
    matchlabels:
      app: angular
  replicas: 5
  template:
    metadata:
      labels:
        app: angular
    spec:
      containers:
      - name: angular
        image:
        ports:
        - containerPort:
---
kind:
apiversion:

